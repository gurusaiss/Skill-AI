# Implementation Plan

- [ ] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Git Push Blocked by Secret Scanning
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For this deterministic bug, scope the property to the concrete failing case: attempting to push commits when `.env` with real API keys exists in Git history
  - Test that `git push origin main` is rejected with "push declined due to repository rule violations" error message
  - Test that `git log --all --full-history -- .env` returns commits containing the `.env` file
  - Test that historical commits contain real API key values (GEMINI_API_KEY and GROQ_API_KEY)
  - The test assertions should match the Expected Behavior Properties from design: push should succeed without secret scanning violations
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found: specific commit hashes containing `.env`, exact error messages from GitHub, exposed key values
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [ ] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Git Workflow and Ignore Rules Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (Git operations that don't involve pushing)
  - Test that `.env` is currently listed in `.gitignore` and is ignored by `git status`
  - Test that `.env.example` is tracked and committable
  - Test that creating a new local `.env` file is ignored by Git (doesn't appear in `git status`)
  - Test that non-sensitive commit history remains intact (verify commit hashes for commits not touching `.env`)
  - Test that branch operations (create, switch, merge) work correctly
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 3. Fix for Git secret exposure in commit history
  - [ ] 3.1 Backup the repository
    - Create a complete backup of the repository before history rewriting
    - Clone to a separate location: `git clone <repo-url> backup-repo` or copy the entire directory
    - Verify backup is complete and contains all branches and history
    - _Bug_Condition: isBugCondition(input) where input.operation == "git push" AND repositoryHistoryContains(".env", ["GEMINI_API_KEY", "GROQ_API_KEY"])_
    - _Expected_Behavior: Push operations succeed without secret scanning violations (Property 1 from design)_
    - _Preservation: Git workflow, .gitignore rules, and non-sensitive commit history remain unchanged (Preservation Requirements from design)_
    - _Requirements: 2.1, 2.2, 2.3_

  - [ ] 3.2 Install git-filter-repo tool
    - Install `git-filter-repo` via pip: `pip install git-filter-repo`
    - Verify installation with `git filter-repo --version`
    - Alternative: Download BFG Repo-Cleaner JAR file if git-filter-repo is not available
    - _Bug_Condition: isBugCondition(input) where input.operation == "git push" AND repositoryHistoryContains(".env", ["GEMINI_API_KEY", "GROQ_API_KEY"])_
    - _Expected_Behavior: Push operations succeed without secret scanning violations (Property 1 from design)_
    - _Preservation: Git workflow, .gitignore rules, and non-sensitive commit history remain unchanged (Preservation Requirements from design)_
    - _Requirements: 2.1, 2.2, 2.3_

  - [ ] 3.3 Remove .env from all historical commits
    - Run: `git filter-repo --path .env --invert-paths --force`
    - This removes `.env` from every commit in the repository history
    - Alternative with BFG: `java -jar bfg.jar --delete-files .env`
    - Verify removal with: `git log --all --full-history -- .env` (should return empty)
    - Verify .gitignore still contains `.env` entry
    - _Bug_Condition: isBugCondition(input) where input.operation == "git push" AND repositoryHistoryContains(".env", ["GEMINI_API_KEY", "GROQ_API_KEY"])_
    - _Expected_Behavior: Push operations succeed without secret scanning violations (Property 1 from design)_
    - _Preservation: Git workflow, .gitignore rules, and non-sensitive commit history remain unchanged (Preservation Requirements from design)_
    - _Requirements: 2.1, 2.2, 2.3, 3.1, 3.2_

  - [ ] 3.4 Force push cleaned history to remote
    - Update remote with cleaned history: `git push origin --force --all`
    - Push all tags if they exist: `git push origin --force --tags`
    - Note: This requires force push permissions on the repository
    - Verify push succeeds without secret scanning violations
    - _Bug_Condition: isBugCondition(input) where input.operation == "git push" AND repositoryHistoryContains(".env", ["GEMINI_API_KEY", "GROQ_API_KEY"])_
    - _Expected_Behavior: Push operations succeed without secret scanning violations (Property 1 from design)_
    - _Preservation: Git workflow, .gitignore rules, and non-sensitive commit history remain unchanged (Preservation Requirements from design)_
    - _Requirements: 2.1, 2.2, 2.3_

  - [ ] 3.5 Rotate exposed API keys
    - Generate new GEMINI_API_KEY at https://aistudio.google.com/app/apikey
    - Generate new GROQ_API_KEY at https://console.groq.com
    - Update local `.env` file with new keys
    - Revoke old keys in respective API consoles to prevent unauthorized use
    - Verify new keys work by testing API calls
    - _Bug_Condition: isBugCondition(input) where input.operation == "git push" AND repositoryHistoryContains(".env", ["GEMINI_API_KEY", "GROQ_API_KEY"])_
    - _Expected_Behavior: Push operations succeed without secret scanning violations (Property 1 from design)_
    - _Preservation: Git workflow, .gitignore rules, and non-sensitive commit history remain unchanged (Preservation Requirements from design)_
    - _Requirements: 2.1, 2.2, 2.3, 3.5_

  - [ ] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Git Push Succeeds Without Secret Violations
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - Verify that `git push origin main` succeeds without errors
    - Verify that `git log --all --full-history -- .env` returns empty (no commits with `.env`)
    - Verify that GitHub secret scanning no longer detects violations
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3_

  - [ ] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - Git Workflow and Ignore Rules Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify that `.env` is still ignored by Git
    - Verify that `.env.example` is still tracked and committable
    - Verify that creating a new local `.env` file is still ignored
    - Verify that non-sensitive commit history remains intact (same commit hashes for non-.env commits)
    - Verify that branch operations still work correctly
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise
  - Verify that push operations succeed without secret scanning violations
  - Verify that all preservation tests pass (Git workflow unchanged)
  - Verify that `.env` no longer exists in Git history
  - Verify that new API keys are working correctly
  - Document any issues or edge cases discovered during testing
